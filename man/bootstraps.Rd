% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstraps.R
\name{bootstraps}
\alias{bootstraps}
\title{Bootstrap resample a nested sampling run}
\usage{
bootstraps(
  x,
  times = 100,
  units = c("original", "unit_cube"),
  apparent = FALSE,
  ...
)
}
\arguments{
\item{x}{[\link{ernest_run}]\cr Results from a nested sampling run.}

\item{times}{\verb{[[integer(1)]]}\cr The number of bootstrap resamples to draw.}

\item{units}{\verb{[character(1)]}\cr The scale of the sampled points:
\itemize{
\item \code{"original"}: Points are on the scale of the prior space.
\item \code{"unit_cube"}: Points are on the (0, 1) unit hypercube scale.
}}

\item{apparent}{\verb{[[logical(1)]]}\cr Whether to include the original run as an
additional resample.}

\item{...}{These dots are for future extensions and must be empty.}
}
\value{
A tibble with one row per resample and these columns:
\itemize{
\item \code{id}: the resample identifier.
\item \code{split}: the sampled thread IDs used to build the resample.
\item \code{run}: an \link{ernest_rcrd} object containing the resampled run.
}

If \code{apparent = TRUE}, an additional \code{Apparent} row is included for the
original run.
}
\description{
Resamples the run's live-point threads with replacement to generate
synthetic runs and estimate uncertainty due to how the nested sampling
algorithm estimates contours within the likelihood surface.
}
\details{
Higson et al. (2019) describe bootstrap resampling for nested sampling.
Here, the run is split into \code{nlive} threads, the threads are sampled with
replacement, and the selected points are merged into synthetic runs.
This gives an empirical estimate of uncertainty from the stochastic choice
of likelihood shells.
}
\examples{
# Generate 100 bootstrap resamples
bootstraps(example_run)
}
\references{
\itemize{
\item Higson, E., Handley, W., Hobson, M., & Lasenby, A. (2019).
Nestcheck: Diagnostic Tests for Nested Sampling Calculations. Monthly Notices
of the Royal Astronomical Society, 483(2), 2044–2056.
\doi{10.1093/mnras/sty3090}
\item Speagle, J. S. (2020). dynesty: A Dynamic Nested Sampling Package for
Estimating Bayesian Posteriors and Evidences. Monthly Notices of the Royal
Astronomical Society, 493, 3132–3158. \doi{10.1093/mnras/staa278}
}
}
\seealso{
\code{\link[=calculate.ernest_run]{calculate.ernest_run()}} for evidence estimation without
bootstrapping.
}
