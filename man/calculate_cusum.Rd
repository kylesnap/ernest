% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosscheck.R
\name{calculate_cusum}
\alias{calculate_cusum}
\title{Visually diagnose sampling issues in an \code{ernest_run}}
\usage{
calculate_cusum(run, width = NULL, omega = 1.5, h = 4, plot = TRUE)
}
\arguments{
\item{run}{[\link{ernest_run}]\cr Results from a nested sampling run.}

\item{width}{\verb{[integer(1)]}\cr Width of the rolling window used to compute
the rank-sum process. Defaults to \code{run$nlive} when left \code{NULL}.}

\item{omega}{\verb{[double(1)]}\cr Reference value used to create the cusum chart.
Larger values make the chart less sensitive to small shifts in the rank sum
of the insertions.}

\item{h}{\verb{[double(1)]}\cr Decision interval for out-of-bounds detection.
Iterations are flagged when the upper CUSUM exceeds \code{h} or the lower CUSUM
drops below \code{-h}.}

\item{plot}{\verb{[logical(1)]}\cr If \code{TRUE}, returns a CUSUM \code{ggplot} object. If
\code{FALSE}, returns the tabular CUSUM values used to make the plot.}
}
\value{
If \code{plot = TRUE}, a \code{ggplot2::ggplot()} object.

If \code{plot = FALSE}, a tibble with columns:
\itemize{
\item \code{.iter}: \verb{[integer()]} CUSUM iteration index.
\item \code{x}: \verb{[double()]} Rolling rank-sum statistic.
\item \code{z}: \verb{[double()]} Standardized rank-sum statistic.
\item \code{.lower}: \verb{[double()]} Lower tabular CUSUM sequence.
\item \code{.upper}: \verb{[double()]} Upper tabular CUSUM sequence.
\item \code{OOB}: \verb{[logical()]} Indicator for whether either CUSUM is outside the
decision interval \link{-h, h}.
}
}
\description{
Issues in sampling caused by poor LRPS behaviour or challenging features
in the model's likelihood may confound nested sampling results. To identify
these issues, this plot tracks the insertion indicies of new points as they
are inserted into the live set, calculates a rolling rank sum statistic,
then creates a CUSUM plot highlighting potentially problematic sampling
iterations.
}
\details{
The diagnostic follows these steps:
\itemize{
\item Compute insertion indices from the run's record of live-point
replacements.
\item Build a rolling rank sum of insertion ranks using \code{width}.
\item Standardize this process using the expected mean and standard deviation
under ideal insertion behavior.
\item Apply a one-sided tabular CUSUM to detect sustained positive or negative
departures from expectation.
}

Out-of-bounds (\code{OOB}) points indicate iterations where insertion behavior may
be inconsistent with well-mixed sampling in the likelihood-restricted prior.
}
\examples{
data(example_run)

# CUSUM diagnostic chart
calculate_cusum(example_run)

# Retrieve CUSUM values for custom plotting or thresholds
head(calculate_cusum(example_run, plot = FALSE))
}
\seealso{
\itemize{
\item \code{\link[=plot]{plot()}} for evidence, weight, and likelihood diagnostics.
\item \code{\link[=visualize]{visualize()}} for posterior density and trace diagnostics.
}
}
