% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slice_rectangle.R
\name{slice_rectangle}
\alias{slice_rectangle}
\title{Slice sampling within a bounding hyperrectangle}
\usage{
slice_rectangle(steps = 3L, enlarge = NA)
}
\arguments{
\item{steps}{\verb{[integer(1)]} Number of reslicing iterations per proposal.}

\item{enlarge}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} \code{enlarge} is now always set
to \code{NA}; sampling regions always begin from the unit hypercube.
Control how frequently \code{sampler} is updated with \code{refresh_frac}.}
}
\value{
\verb{[slice_rectangle]} A list inheriting from \code{ernest_lrps}.
}
\description{
Propose new live points by evolving an existing live point using slice
sampling inside a bounding hyperrectangle. The rectangle is shrunk when a
proposal is rejected.
}
\details{
Sampling starts from a live point that meets the likelihood criterion. Each
iteration draws a uniform proposal inside the rectangle and accepts it if it
satisfies the criterion. Rejected proposals cause the rectangle to be
shrunk so the rejected point lies on the boundary and the original point
remains inside. The process repeats until a valid proposal is found or the
rectangle can no longer be reduced.

Use \code{steps} to increase how many times a point goes through the slicing
procedure before being returned as a sample for the live set. One step
consists of one or more slicing operations, and each slice proceeds until
a point is found that satisfies the likelihood criterion.
}
\section{Loop safety}{

An LRPS may fail to produce a point satisfying the likelihood constraint
(for example, due to rounding errors or poor parameterisation). To avoid
infinite loops during a nested sampling run, \code{propose()} will terminate a
run if it cannot find a likelihood-restricted point after a maximum number of
calls to the likelihood function. Adjust this limit with the
\code{ernest.max_loop} option (default: 1e6).
}

\examples{
lrps <- slice_rectangle()
lrps

}
\references{
Neal, R. M. (2000). Slice Sampling (Version 1). arXiv.
\doi{10.48550/ARXIV.PHYSICS/0009028}
Buchner, J. (2021). UltraNest - A Robust, General Purpose Bayesian Inference
Engine. Journal of Open Source Software, 6(60), 3001.
\doi{10.21105/joss.03001}
}
\seealso{
Other ernest_lrps:
\code{\link[=multi_ellipsoid]{multi_ellipsoid()}},
\code{\link[=rwmh_cube]{rwmh_cube()}},
\code{\link[=unif_cube]{unif_cube()}},
\code{\link[=unif_ellipsoid]{unif_ellipsoid()}}
}
\concept{ernest_lrps}
