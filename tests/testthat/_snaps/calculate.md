# calculate handles unsorted log_lik

    `log_lik` must be a vector in ascending order; cannot estimate integral.

# calculate handles unsorted log_vol

    `log_lik` must be a vector in ascending order; cannot estimate integral.

