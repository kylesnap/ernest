# create_likelihood with auto_batch

    Code
      ll
    Output
      An <ernest_likelihood> function:
      > `error_action` = "abort"
      > `nonfinite_action` = "warn"
      
      function (x) 
      -sum((x - 1)^2)

