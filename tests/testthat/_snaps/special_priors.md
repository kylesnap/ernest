# create_normal_prior / reports poor parameters

    Code
      create_normal_prior(sd = -1)
    Condition
      Error in `create_normal_prior()`:
      ! All elements of `sd` must be strictly positive and non-missing.

